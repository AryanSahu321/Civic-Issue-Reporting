
 ### All the tasks that we have done right till now, from small to small and last to last, create a table so that I can revise the important decisions that I have made and its reason and to know where my project is right now and how many things I have done.

------
Yahan aapke ab tak kiye gaye sabhi decisions, tasks, aur project status ki summary table di gayi hai, jisse aap easily revise kar sakein:

| Step / Phase | Action / Decision | Reason / Purpose | Current Status / Result |
| --- | --- | --- | --- |
| **1. Framework & Language Choice** | Decided to use **TypeScript & Industry Standards** for the backend. | Long-term code stability, early bug detection, and clean enterprise architecture. | Selected and configured via `tsconfig.json`. |
| **2. Git Cleanup** | Removed accidental parent-level Git repo (`civic_issue`) and `.git` folder. | To avoid nested repository conflicts and future GitHub synchronization errors. | Cleaned up; root correctly configured only at `Civic-Issue-Reporting`. |
| **3. Node Modules Ignore** | Added `node_modules` to `.gitignore` via VS Code prompt. | To prevent Git from overloading with thousands of dependency files and throwing change warnings. | Successfully configured. |
| **4. Database Selection** | Decided to install and use **PostgreSQL**. | Required for advanced spatial queries (PostGIS) which Oracle does not natively support for this use case. | Steps provided for installation and setup. |
| **5. Prisma Setup** | Installed Prisma ORM and initialized `schema.prisma`. | To act as a secure translator between Node.js code and PostgreSQL without writing raw SQL queries. | Installed, configuration file ready. |
| **6. Folder Architecture** | Planned and executed industry-standard folder structure (`src/controllers`, `src/routes`, `src/services`, `src/middlewares`). | To maintain clean separation of concerns and prevent team merge conflicts. | Structure ready for code implementation. |

### Project Current Status

* **Phase:** Core Backend Setup & Database Integration.
* **Next Action:** Complete PostgreSQL installation, update the `.env` connection string, run `npx prisma db push`, and start writing the OTP Authentication Controller.

-------

Yahan aapke Civic-Issue-Reporting project ke current progress ke adhaar par 3 professional tracking tables di gayi hain. Aap inhe copy karke apne GitHub repository ki documentation (`PROJECT_LOGS.md`) mein save kar sakte hain:


Neeche ek **Reusable Master Prompt** diya gaya hai. Aap isay copy karke apne paas save kar sakte hain. Jab bhi aapko project ka status, ADRs, ya issue/error tracking table chahiye ho, aap bas yeh prompt mujhe bhej dijiyega:

---

### 📋 Reusable Master Prompt (Copy This)

```text
Please generate the comprehensive project summary and tracking reports for our Civic-Issue-Reporting project based on our current progress. 

Please structure the output into the following 3 detailed tables with all standard industry parameters:

1. Table 1: Task & Ticket Tracker (Kanban / Issue Tracker state: To Do, In Progress, Done, small-to-small tasks, and assignee/module details).
2. Table 2: Architecture Decision Records (ADRs) & Trade-offs (Decision name, context, technical justification/reasons, and alternatives considered).
3. Table 3: Error & Bug Tracking Log (Known errors encountered, root cause, resolution status, and prevention tools like Sentry/Logs).

Ensure all parameters, technical decisions, small tasks, and reasons are captured accurately so I can maintain it in my GitHub repository documentation.

```

---

### Is Prompt ka Fayda:

* Jab bhi aap ise use karenge, main aapko exact 3 structured tables (Tickets/Tasks, ADRs, aur Error Logs) bana kar doonga.
* Aap in tables ko copy karke apne GitHub repo mein `PROJECT_LOGS.md` ya `DOCUMENTATION.md` naam ki file mein paste kar sakte hain.

Aap ise save kar lijiye aur jab bhi revision ya update chahiye ho, mujhe yeh prompt bhej dijiye!

### Table 1: Task & Ticket Tracker (Kanban / Issue Tracker)

| Ticket ID | Task Description | Module / Area | Assignee | Status |
| --- | --- | --- | --- | --- |
| **TCK-001** | Initialize Node.js backend project & install core dependencies (`express`, `cors`, `dotenv`) | Backend Core | Team Lead / Member 2 | **Done** |
| **TCK-002** | Configure TypeScript and project folder structure (`src/controllers`, `src/routes`, etc.) | Architecture | Team Lead / Member 2 | **Done** |
| **TCK-003** | Clean up accidental parent-level Git repo (`civic_issue`) and add `.gitignore` for `node_modules` | Version Control | Team Lead | **Done** |
| **TCK-004** | Install and configure Prisma ORM with PostgreSQL database schema (`schema.prisma`) | Database | Team Lead | **In Progress** |
| **TCK-005** | Set up local PostgreSQL server and update connection string in `.env` | Database / DevOps | Team Lead | **To Do** |
| **TCK-006** | Implement Mobile OTP + JWT Authentication Controller and Routes | Auth Service | Team Lead | **To Do** |

---

### Table 2: Architecture Decision Records (ADRs) & Trade-offs

| ADR ID | Decision Name | Context & Problem | Technical Justification / Reasons | Alternatives Considered |
| --- | --- | --- | --- | --- |
| **ADR-001** | **TypeScript over Plain JavaScript** | Choosing a programming language for backend scalability and maintainability. | Enables static typing, catches bugs at compile-time rather than runtime, and provides better IDE support for team collaboration. | Plain JavaScript (flexible but prone to type-mismatch bugs in large teams). |
| **ADR-002** | **PostgreSQL + PostGIS over Oracle DB** | Selecting a primary database that handles geographical data. | PostGIS extension is required for spatial radius checks (< 10 meters) which Oracle does not natively support as seamlessly for this architecture. | Oracle Database (already installed locally, but lacked native spatial ecosystem support for our specific microservice needs). |
| **ADR-003** | **Prisma ORM for Database Management** | Managing database queries safely without raw SQL overhead. | Type-safe queries, automated migrations, and clean schema definition that acts as a single source of truth. | Mongoose / TypeORM / Raw SQL drivers. |

---

### Table 3: Error & Bug Tracking Log

| Error ID | Error Description / Encountered Issue | Root Cause | Resolution Status | Prevention / Tracking Tool |
| --- | --- | --- | --- | --- |
| **ERR-001** | `ERESOLVE could not resolve dependency conflict` when installing `prisma` alongside `ts-jest`. | Version mismatch and strict peer dependency checks in newer NPM versions against TypeScript/Jest packages. | **Resolved** (Used `--legacy-peer-deps` flag to bypass strict tree conflicts). | NPM Cache & Error Logs |
| **ERR-002** | `Error: P1001 Can't reach database server at localhost:51214` during Prisma initialization. | PostgreSQL server was either not running or the connection URL port in `.env` did not match the local running instance. | **In Progress** (Guiding local PostgreSQL installation and config). | Prisma DB Connection Diagnostics / Console Logs |
