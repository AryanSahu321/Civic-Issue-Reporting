
 ### All the tasks that we have done right till now, from small to small and last to last, create a table so that I can revise the important decisions that I have made and its reason and to know where my project is right now and how many things I have done. provide githum markdown only after old tables 
 

------
Yahan aapke ab tak kiye gaye sabhi decisions, tasks, aur project status ki summary table di gayi hai, jisse aap easily revise kar sakein:

| Step / Phase | Action / Decision | Reason / Purpose | Current Status / Result |
| --- | --- | --- | --- |
| **1. Framework & Language Choice** | Decided to use **TypeScript & Industry Standards** for the backend. | Long-term code stability, early bug detection, and clean enterprise architecture. | Selected and configured via `tsconfig.json`. |
| **2. Git Cleanup** | Removed accidental parent-level Git repo (`civic_issue`) and `.git` folder. | To avoid nested repository conflicts and future GitHub synchronization errors. | Cleaned up; root correctly configured only at `Civic-Issue-Reporting`. |
| **3. Node Modules Ignore** | Added `node_modules` to `.gitignore` via VS Code prompt. | To prevent Git from overloading with thousands of dependency files and throwing change warnings. | Successfully configured. |
| **4. Database Selection** | Decided to install and use **PostgreSQL**. | Required for advanced spatial queries (PostGIS) which Oracle does not natively support for this use case. | Steps provided for installation and setup. |
| **5. Prisma Setup** | Installed Prisma ORM and initialized `schema.prisma`. | To act as a secure translator between Node.js code and PostgreSQL without writing raw SQL queries. | Installed, configuration file ready. |
| **6. Folder Architecture** | Planned and executed industry-standard folder structure (`src/controllers`, `src/routes`, `src/services`, `src/middlewares`). | To maintain clean separation of concerns and prevent team merge conflicts. | Structure ready for code implementation. |

### Project Current Status

* **Phase:** Core Backend Setup & Database Integration.
* **Next Action:** Complete PostgreSQL installation, update the `.env` connection string, run `npx prisma db push`, and start writing the OTP Authentication Controller.

-------

| Step / Phase | Action / Decision | Reason / Purpose | Current Status / Result |
| :--- | :--- | :--- | :--- |
| **7. Dependency Resolution & ESM Standard** | Resolved strict npm peer conflicts using `--legacy-peer-deps` and set `"type": "module"` in `package.json`[cite: 1, 3]. | Avoids package collisions (e.g., `ts-jest` vs `typescript`) and aligns with modern Node.js ECMAScript Modules[cite: 1, 3]. | Configured successfully in `package.json`[cite: 1, 3]. |
| **8. TypeScript & Dev Runner Configuration** | Set `"verbatimModuleSyntax": false` in `tsconfig.json` and migrated dev script from `ts-node` to `tsx watch`[cite: 1, 3]. | Fixes type-only import strictness errors and ensures crash-free TypeScript execution in ESM mode[cite: 1, 3]. | Configured and operational in npm scripts[cite: 1, 3]. |
| **9. Database Schema & Client Sync** | Configured local PostgreSQL credentials in `.env`, ran `npx prisma db push`, and executed `npx prisma generate`[cite: 1, 3]. | Establishes the initial database tables (`User`, `Post`) and builds the typed Prisma Client[cite: 1, 3]. | Database schema synchronized and Prisma Client generated[cite: 1, 3]. |
| **10. Authentication Architecture Decision** | Selected Mobile Number + OTP with JWT Role-Based Access Control (RBAC) instead of Aadhaar verification[cite: 1, 3]. | Minimizes onboarding drop-off, eliminates regulatory compliance overhead, and protects citizen privacy[cite: 1, 3]. | Architecture locked; ready for route/controller binding[cite: 1, 3]. |
| **11. AI Context & Gem Guardrails** | Established custom Gemini Gem configuration, role prompts, and unified documentation standards (`DOCUMENTATION.md`)[cite: 1, 3]. | Eliminates AI hallucination and architectural drift, enforcing strict PostGIS, gRPC, and ESM standards[cite: 1, 3]. | Framework, system instructions, and templates established[cite: 1, 3]. |

### Project Current Status

* **Phase:** Authentication Engine Implementation & API Routing (`auth-service`)[cite: 1, 3].
* **Next Action:** Build `src/controllers/auth.controller.ts` (OTP request and verification endpoints), mount routes in `src/routes/auth.routes.ts`, and test JWT token issuance[cite: 1, 3].
